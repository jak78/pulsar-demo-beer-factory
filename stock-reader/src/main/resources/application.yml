
server:
  port: 9100
logging:
  level:
    root: WARN
    io.streamnative: INFO
    org.apache.pulsar.common.util.netty: ERROR
spring:
  pulsar:
    defaults:
      type-mappings: 
        - message-type: io.streamnative.pulsarbeerfactory.warehouse.BeerStock
          schema-info:
            schema-type: AVRO
