server:
  port: 9090
logging:
  level:
    root: WARN
    io.streamnative: INFO
    org.apache.pulsar.common.util.netty: ERROR
spring:
  pulsar:
    defaults:
      type-mappings: 
        - message-type: io.streamnative.pulsarbeerfactory.website.DeliveryOrder
          schema-info:
            schema-type: JSON
